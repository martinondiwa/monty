#include "monty.h"

/**
 * f_stack - Sets the data structure to stack mode.
 * @head: Pointer to the stack head.
 * @counter: Line number (unused).
 *
 * Return: No return.
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}

